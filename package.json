{
  "name": "phone-catalog",
  "version": "1.0.0",
  "description": "Phone catalog",
  "scripts": {
    "install-modules": "node install.js",
    "start": "concurrently --kill-others \"npm run server\" \"npm run client\"",
    "server": "node start-server.js",
    "client": "node start-client.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "babel-preset-env": "1.7.0",
    "babel-register": "6.26.0",
    "concurrently": "4.1.0"
  },
  "dependencies": {
    "express": "4.16.4",
    "fs": "0.0.1-security",
    "json-server": "0.14.2"
  }
}
