const scope = 'containers/Catalog';

export const GET_PHONES = `${scope}/GET_PHONES`;
export const SET_PHONES = `${scope}/SET_PHONES`;
export const ERROR = `${scope}/ERROR`;

export const REDUCER_KEY = 'catalog';
export const SAGA_KEY = 'catalog';

